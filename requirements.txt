# The DASHBOARD environment only. Streamlit Community Cloud installs from this file.
#
# It deliberately does not include hopsworks: Streamlit needs protobuf>=5 and the
# Hopsworks SDK needs protobuf<5, so they cannot share an environment. The dashboard
# talks to the feature store through the API (set AQI_API_URL), which is how it was
# designed to work anyway. Everything else installs from pyproject extras:
#
#   pip install -e ".[store,explain,api,dev]"        pipelines + Flask/FastAPI + tests
#   pip install -e ".[deep]"                          adds TensorFlow 2.17 (protobuf<5)
#   pip install -e ".[dashboard]"                     this file, as an extra
pandas>=2.1
numpy>=1.26
requests>=2.31
python-dotenv>=1.0
scikit-learn>=1.4
joblib>=1.3
pyarrow>=15.0
shap>=0.44
matplotlib>=3.8
streamlit>=1.49
plotly>=5.20
httpx>=0.27
